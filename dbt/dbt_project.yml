name: is_rock_alive

profile: is_rock_alive

seed-paths: ["seeds"]
model-paths: ["models"]
macro-paths: ["macros"]
clean-targets:
  - "target"
  - "dbt_packages"

models:
  is_rock_alive:
    # Materialize staging models as views, and marts as tables
    staging:
      +materialized: view
    trusted:
      +materialized: table
    semantic:
      +materialized: table